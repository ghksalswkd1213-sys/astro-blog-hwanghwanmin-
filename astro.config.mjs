// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 배포 후 커스텀 도메인을 붙일 때 이 값만 바꾸면 됩니다.
  // 예: 'https://myblog.com' 또는 'https://astro-blog.pages.dev'
  site: 'https://example.pages.dev',
});
